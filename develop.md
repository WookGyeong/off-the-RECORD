# OTR 개발 일지

이 문서는 OTR 프로젝트를 만들면서 "무엇을, 왜, 어떻게" 바꿨는지 기록하는 개발 일지예요.
개발을 처음 배우는 사람도 따라올 수 있도록 최대한 쉬운 말로 씁니다.

- 최신 변경사항이 **맨 위**에 오도록 계속 추가할게요.
- 코드를 직접 안 열어봐도 이 파일만 읽으면 "지금 앱이 어떤 상태인지" 알 수 있는 걸 목표로 해요.

---

## 이 프로젝트는 뭔가요?

**OTR (Off The Record)** 은 동네 골목이나 오래된 가게에 숨은 이야기를 오디오로 들려주는
하이퍼로컬(아주 좁은 지역 기반) 오디오 가이드 앱이에요.

- 프레임워크: **Next.js** (리액트 기반 웹 프레임워크)
- 스타일: **Tailwind CSS**
- 언어: **TypeScript**

## 폴더 구조 (대략)

```
app/                Next.js 페이지들 (화면 단위)
  page.tsx          메인 홈 화면 (지도 + 카테고리 필터 + 코스 목록)
  add-place/        장소 추가 화면 (지도 핀 + 이름/설명/카테고리 입력)
  recycle/          다른 페이지
components/otr/     화면을 구성하는 부품(컴포넌트)들
data/otr/           스팟(가게), 코스 같은 데이터
```

## 지금 실행해보는 법

```bash
npm run dev
```

실행 후 브라우저에서 `http://localhost:3000` 접속

## 앞으로 할 일 (TODO)

- [x] 카카오 개발자 사이트에서 JavaScript 키 발급받아 등록하기
- [x] 카카오맵 개발자 콘솔에 `http://localhost:3000` / 실제 배포 도메인(`https://otr-lion.vercel.app`) 등록하기
- [ ] 스팟(가게) 좌표를 임시값 대신 실제 정확한 주소/좌표로 교체하기

---

## 변경 기록

### 2026-08-20 — 검색 결과 지도를 목록 맨 아래가 아니라 클릭한 항목 바로 밑에 뜨도록 수정

**무엇을 했나요?**
- 바로 전에 만든 검색 결과 지도가 항상 목록 전체의 맨 아래에만 떴었는데, 어떤 항목을 클릭하든 위치가 고정이라 "내가 지금 뭘 눌렀는지"와 지도가 시각적으로 안 이어져 보였어요.
- 그래서 지도가 **클릭한 그 항목 바로 아래**에 붙어서 열리도록(아코디언처럼) 바꿨어요. 다른 항목을 누르면 원래 열려있던 지도는 닫히고, 새로 누른 항목 아래에 지도가 다시 열려요. 같은 항목을 다시 누르면 접혀요.
- `components/otr/SearchSheet.tsx` — 지도를 목록 바깥의 별도 영역이 아니라, 각 결과 `<li>` 안에서 `isSelected`일 때만 그 항목 바로 아래에 렌더링하도록 구조 변경
- Playwright로 첫 항목 클릭 → 그 항목 밑에 지도 등장, 두 번째 항목 클릭 → 첫 지도는 닫히고 두 번째 항목 밑에 새 지도 등장, 같은 항목 다시 클릭 → 접히는 것까지 스크린샷으로 확인. 콘솔 에러 없음.

### 2026-08-20 — 메인 화면에 스팟 검색 기능 추가

**무엇을 했나요?**
- 헤더의 돋보기(검색) 버튼을 누르면 그동안 "준비 중이에요" 토스트만 뜨던 걸, 실제로 동작하는 **검색 시트**가 화면 아래에서 올라오도록 만들었어요.
- 검색 시트 안에는 순서대로:
  1. **검색 범위 다이얼** — `/add-place`의 카테고리 다이얼과 같은 방식(가로로 넘기거나 탭으로 선택)으로 "제목 / 내용 / 제목+내용" 중 하나를 골라요. 기본값은 **제목**이에요.
  2. **검색어 입력창 + 검색 버튼** — 버튼을 누르면(또는 Enter) 그 아래에 결과 목록이 떠요.
  3. **결과 목록** — 각 항목에 카테고리 배지와 스팟 이름이 보여요. 지금은 스팟의 이름/설명 텍스트를 그대로 검색해요(카카오 장소 검색이 아니라 우리 앱 안의 데이터를 찾는 거예요).
  4. 목록에서 하나를 누르면 **목록 바로 아래에 그 장소 하나만 핀으로 찍힌 작은 지도**가 나타나고, 다른 항목을 누르면 이전 지도는 사라지고 새로 누른 장소의 지도로 바뀌어요. (지도는 한 번에 하나만 보여요.)
- `components/otr/SearchScopeDial.tsx` (신규) — 제목/내용/제목+내용 선택용 다이얼
- `components/otr/SearchResultMap.tsx` (신규) — 검색 결과 하나를 위한 미니 지도 + 카테고리 색 핀
- `components/otr/SearchSheet.tsx` (신규) — 위 두 컴포넌트를 묶은 검색 바텀시트 전체
- `app/page.tsx` — 검색 버튼이 `SearchSheet`를 열도록 연결하고, 배경 딤(어둡게) 처리와 "바깥 클릭하면 닫기"를 기존 스팟/코스 시트와 같은 로직에 합쳤어요.
- Playwright로 "골목"을 제목 검색 → 결과 2개 확인, 결과 클릭 → 지도 뜨는 것, 다른 결과 클릭 → 지도가 새 장소로 바뀌는 것(지도가 두 개로 안 겹치는 것), "내용" 범위로 바꿔서 "세탁" 검색 → 설명에 세탁이 들어간 스팟만 나오는 것, X 버튼으로 닫히는 것까지 확인했어요. 콘솔 에러 없음.

**기억해둘 점 (초보자 팁)**
- 이건 카카오 장소 검색이 아니라 **우리 앱에 이미 등록된 스팟(spots.ts + 사용자가 추가한 곳)만** 찾는 검색이에요. 어제 만든 "장소 추가" 화면의 검색(카카오 실제 장소 DB 검색)이랑 이름은 비슷하지만 완전히 다른 기능이니 헷갈리지 않게 주의!

### 2026-08-20 — 검색창 바깥 클릭 시 닫기 + 핀 이동하면 주변 장소 추천

**무엇을 했나요?**
- 어제 만든 장소 검색(`PlaceSearch`)에서, 검색 결과 목록이 뜬 채로 화면의 다른 곳을 클릭하면 목록이 그대로 남아있던 걸 고쳤어요. 이제 검색창/목록 바깥 아무 곳이나 클릭하면 목록이 닫혀요. (`document`에 클릭 감지를 걸어두고, 클릭한 지점이 검색 영역 바깥이면 닫도록 했어요.)
- 지도에서 핀을 손으로 움직여서 위치를 맞추면(드래그가 끝나고 지도가 멈추면), **그 위치와 가장 가까운 실제 장소**를 자동으로 찾아서 "이 위치와 가장 비슷한 장소: OO (약 27m)" 같은 추천 카드를 보여주도록 했어요.
  - 카카오의 **카테고리별 장소 검색**(`categorySearch`)을 음식점·카페·편의점·마트·문화시설·관광명소·약국·병원 등 여러 카테고리로 동시에 돌려서, 핀 반경 120m 안에서 가장 가까운 곳 하나를 골라 보여줘요.
  - "이 이름 쓰기"를 누르면 그 장소 이름이 자동으로 "장소 이름" 칸에 채워지고, 핀도 그 장소의 정확한 좌표로 살짝 이동해요(사용자가 대충 찍은 위치를 실제 장소 좌표로 보정). ✕를 누르면 그냥 무시할 수 있어요.
  - 처음 구현했을 때는 추천을 "쓰기"로 선택하면 지도가 그 좌표로 다시 이동(panTo)하면서 이동 이벤트가 또 발생해 같은 추천이 다시 떠버리는 무한 반복 버그가 있었는데, "지금 이 지도 이동은 사람이 드래그한 게 아니라 코드가 만든 이동"이라는 표시(플래그)를 하나 추가해서 그 경우엔 추천을 다시 찾지 않도록 고쳤어요.
- Playwright로 검색 결과가 뜬 상태에서 다른 곳 클릭 → 목록이 실제로 사라지는 것, 지도를 드래그 → 추천 카드가 뜨는 것, "이 이름 쓰기" 클릭 → 이름이 채워지고 추천 카드가 다시 뜨지 않는 것까지 확인했어요. 콘솔 에러 없음.

**바뀐 파일들**
- `components/otr/PlaceSearch.tsx` — 바깥 클릭 감지(click-away)로 검색 결과 닫기 추가
- `components/otr/AddPlaceMap.tsx` — 핀이 멈추면 주변 장소를 찾아 추천하는 로직 추가, 프로그램이 만든 지도 이동과 사용자가 드래그한 이동을 구분하는 플래그 추가
- `app/add-place/page.tsx` — 추천 카드 UI 추가("이 이름 쓰기" / 닫기)

**기억해둘 점 (초보자 팁)**
- "카테고리별 검색"은 음식점/카페/편의점/약국처럼 정해진 카테고리 목록 안에서만 찾기 때문에, 세탁소나 문구점처럼 정식 카테고리가 없는 업종은 추천에 안 뜰 수 있어요. 이런 곳은 추천 없이 직접 이름/설명을 입력하면 돼요.
- "지도가 코드 때문에 움직였는지, 사람이 드래그해서 움직였는지" 구분하는 게 생각보다 중요해요. 구분을 안 하면 코드가 지도를 이동시킬 때마다 이벤트가 또 발생해서 의도치 않은 동작(이번엔 추천 재등장)이 반복될 수 있어요.

### 2026-08-19 — "장소 추가" 화면에 실제 장소 검색 기능 추가

**무엇을 했나요?**
- 지금까지는 `/add-place`에서 지도를 손으로 움직여서 핀 위치를 맞추는 방식만 있었어요. 그런데 여러 사람이 각자 손으로 핀을 찍으면, 같은 곳을 가리켜도 좌표가 조금씩 다르고 이름 표기도 제각각이라 "이게 같은 장소인지" 구분하기 어려워지는 문제가 있었어요.
- 그래서 카카오맵 JavaScript SDK에 원래 들어있는 **장소 키워드 검색 서비스**(`kakao.maps.services.Places`)를 새로 연결해서, 카카오맵 앱에서 장소를 검색하는 것과 똑같은 방식으로 "가게 이름 검색 → 목록에서 정확한 곳 선택"이 가능하게 만들었어요.
  - `components/otr/PlaceSearch.tsx`를 새로 만들었어요. 검색창에 이름을 입력하고 검색하면, 카카오의 실제 장소 DB에서 결과 목록(이름 + 주소)을 보여줘요.
  - 목록에서 하나를 고르면 그 장소의 정확한 좌표로 지도가 자동으로 이동하고, "장소 이름" 입력칸에도 이름이 자동으로 채워져요. (원한다면 이름/설명은 그대로 손으로 수정 가능)
  - "사라진 지물포 자리"처럼 지금은 공식 장소가 아닌 곳은 검색이 안 되니까, 그럴 땐 지금 그 자리를 대신 차지하고 있는 가게(검색 가능한 곳)를 검색해서 정확한 위치를 잡고, "장소 설명"에 "예전엔 지물포였다"는 이야기를 적으면 돼요.
  - 검색 없이 지도를 손으로 움직여서 핀을 맞추는 기존 방식도 그대로 남아있어요(공식 장소가 아예 없는 골목 같은 곳을 위해).
- `components/otr/AddPlaceMap.tsx`를 검색 결과로 지도를 다시 가운데로 옮길 수 있도록 고쳤어요(기존엔 지도 → 화면 방향으로만 좌표를 알려줬는데, 이제 화면 → 지도 방향으로도 좌표를 넣어줄 수 있어요). 카카오맵 스크립트 로딩도 `/add-place` 페이지 하나에서만 하도록 정리했고, 검색 기능을 쓰려면 필요한 `libraries=services` 옵션을 스크립트 주소에 추가했어요.
- Playwright로 실제로 "스타벅스 대구"를 검색 → 결과 목록 확인 → 하나 선택 → 지도가 정확히 그 위치로 이동하고 이름 칸이 채워지는 것까지 확인했어요. 콘솔 에러 없음.

**바뀐 파일들**
- `components/otr/PlaceSearch.tsx` (신규) — 장소 키워드 검색 UI + 카카오 Places 서비스 연동
- `components/otr/AddPlaceMap.tsx` — 지도를 외부(검색 선택)에서도 움직일 수 있게 변경, 자체 스크립트 로딩 제거
- `app/add-place/page.tsx` — 카카오맵 스크립트를 페이지 레벨에서 한 번만 로드(`libraries=services` 추가), 검색 컴포넌트 연결

**기억해둘 점 (초보자 팁)**
- 카카오맵 SDK는 기본 지도 기능 말고도 "services"라는 별도 라이브러리를 켜야 장소 검색/좌표 변환 같은 기능을 쓸 수 있어요. `?libraries=services`처럼 스크립트 주소에 옵션을 추가해줘야 해요.
- 지금은 좌표를 검색으로 정확하게 맞출 수 있게만 해준 거고, "여러 사람이 등록한 서로 다른 장소를 자동으로 합치는" 기능은 아직 없어요. 나중에 여러 명이 동시에 장소를 등록하는 구조로 가면, 같은 좌표(또는 아주 가까운 좌표)에 이미 등록된 장소가 있는지 먼저 확인해서 중복 등록을 막는 로직이 필요해질 거예요.

### 2026-08-19 — 메인 화면 문구 삭제 + 카테고리·코스 가로 스크롤 고치기

**무엇을 했나요?**
- 메인 화면 상단의 "주변 탐색 · 약전골목 인근" 안내 문구를 지웠어요.
- 카테고리 필터(전체·동네·골목·문화·역사·카페·찻방)와 "지금 걷기 좋은 코스" 목록이 화면 밖으로 잘려서 넘어가는 문제를 고쳤어요.
  - 실제로 확인해보니 두 영역 다 기술적으로는 가로 스크롤이 되는 상태였는데(`scrollWidth > clientWidth`), 스크롤바를 아예 숨겨놓은 데다(`no-scrollbar`) 일반 마우스 휠(세로 스크롤)로는 넘길 방법이 없어서 "고장난 것처럼" 보였던 거였어요. (터치(손가락)로 좌우로 미는 건 원래도 됐어요.)
  - 마우스 휠을 위아래로 굴리면 그 힘을 가로 스크롤로 바꿔주는 코드를 두 컴포넌트에 추가했어요(`onWheel` 이벤트에서 `deltaY`만큼 `scrollLeft`를 움직임).
  - 스크롤바는 평소엔 투명하게 숨겨두고, 그 영역에 마우스를 올리거나(hover) 포커스가 갔을 때만 얇게 보이도록 CSS를 새로 만들었어요(`.hscroll` 클래스). 완전히 숨기지 않은 이유는, 마우스로 스크롤바를 직접 눌러서 끌 수도 있게 하기 위해서예요.
- Playwright(자동화 브라우저)를 이 프로젝트에 직접 설치해서, 모바일 화면 크기(390px)로 실제 렌더링을 스크린샷으로 찍어 수정 전/후를 비교했고, 마우스 휠 스크롤이 실제로 동작하는 것까지 확인했어요.

**바뀐 파일들**
- `app/page.tsx` — 상단 안내 문구 삭제
- `app/globals.css` — 호버할 때만 보이는 얇은 스크롤바 스타일(`.hscroll`) 추가
- `components/otr/CategoryFilter.tsx` — `hscroll` 클래스 적용 + 휠 스크롤 핸들러 추가
- `components/otr/CourseRail.tsx` — `hscroll` 클래스 적용 + 휠 스크롤 핸들러 추가

**기억해둘 점 (초보자 팁)**
- `overflow-x: auto`가 걸려 있어도 "스크롤이 가능하다"는 게 사용자에게 눈에 보이지 않으면(스크롤바도 없고 휠도 안 먹으면) 사람들은 그냥 고장났다고 생각해요. 특히 데스크톱에서 트랙패드가 아니라 일반 마우스 휠만 쓰는 사람은 가로 스크롤 영역을 절대 못 넘길 수 있어요.
- 모바일(터치) 환경에서는 원래도 브라우저가 손가락 드래그를 가로 스크롤로 알아서 처리해주기 때문에 이번 수정과 상관없이 잘 동작해요. 이번에 고친 건 "마우스로 테스트/사용할 때"의 접근성이에요.

### 2026-08-18 — 새로 추가한 장소를 새로고침해도 안 사라지게 저장 (localStorage)

**무엇을 했나요?**
- 바로 아래 항목("+ 장소 추가" 버튼)에서 만든 장소가 새로고침하면 사라지던 문제를 고쳤어요.
- 사용자가 `/add-place`에서 등록한 장소를 브라우저의 **localStorage**(브라우저에 데이터를 저장해두는 공간)에 저장하도록 했어요.
  - 앱이 켜질 때(브라우저에서만) localStorage에 저장된 장소를 읽어서, 원래 있던 6개 스팟 뒤에 이어 붙여요.
  - 새 장소를 등록할 때마다 사용자가 추가한 장소 목록 전체를 다시 localStorage에 저장해요.
  - 저장된 데이터가 깨져 있거나 형식이 이상하면(예: 카테고리 값이 이상함) 그 항목은 무시하고 정상인 것만 불러오도록 안전장치를 넣었어요.
- 서버에서 화면을 미리 그리는 단계(SSR)에서는 브라우저 저장소에 접근할 수 없기 때문에, `typeof window === 'undefined'`로 "지금 서버인지 브라우저인지"를 먼저 확인하고 브라우저일 때만 localStorage를 사용해요.
- Playwright로 장소를 등록한 뒤 페이지를 실제로 새로고침(reload)해도 핀 개수가 그대로 유지되는 것과, localStorage에 데이터가 잘 들어가 있는 것을 확인했어요.

**바뀐 파일들**
- `data/otr/spots.ts` — `addSpot`이 localStorage에도 저장하도록 변경, 앱 시작 시 localStorage에서 사용자가 추가한 장소를 불러오는 로직 추가

**기억해둘 점 (초보자 팁)**
- localStorage는 **"이 브라우저, 이 기기에만"** 저장돼요. 다른 사람 컴퓨터나 다른 브라우저에서는 안 보여요. 여러 사람이 등록한 장소를 다 같이 보려면 결국 서버 + 데이터베이스가 필요해요.
- 시크릿(프라이빗) 모드나 브라우저 저장 공간이 꽉 찬 경우 localStorage 저장이 실패할 수 있는데, 이때도 앱이 에러로 멈추지 않고 그냥 저장을 건너뛰도록 만들었어요.

### 2026-08-18 — "+ 장소 추가" 버튼과 장소 등록 화면 추가

**무엇을 했나요?**
- 메인 화면 우측 상단(지도 바깥, 헤더 영역)에 **"+ 장소 추가"** 버튼을 새로 만들었어요. 누르면 `/add-place` 화면으로 이동해요.
- `/add-place` 화면을 새로 만들었어요.
  - 카카오맵이 뜨고, 지도 가운데에는 **핀이 화면에 고정**되어 있어요. 사용자가 지도를 손으로 움직이면(드래그하면) 지도만 움직이고, 핀은 항상 화면 중앙에 그대로 있어서 "핀이 가리키는 위치 = 지도 중앙"이 되는 방식이에요. 지도가 멈추면(`idle` 이벤트) 그 순간의 위/경도를 저장해요.
  - 지도 아래에 **장소 이름**(한 줄 입력), **장소 설명**(여러 줄 입력) 칸을 넣었어요.
  - **카테고리**는 기존 4개 목록(동네·골목·문화·역사·카페·찻방)을 가로로 옆으로 넘기는 **다이얼(스크롤 스냅) 방식**으로 골라요. 가운데 칸에 들어온 카테고리가 선택되고, 원하는 칸을 손가락으로 툭 눌러도 그 칸이 가운데로 이동하며 바로 선택돼요.
  - "이 위치에 장소 등록하기"를 누르면 입력한 이름/설명/카테고리와 지도 중앙 좌표로 새 스팟을 만들고, 메인 화면으로 돌아가요. 돌아가면 방금 등록한 장소의 핀이 지도에 바로 나타나요.
- 새로 만든 장소는 아직 오디오 가이드가 없기 때문에(`audioDuration: 0`), 스팟 상세 화면(`SpotSheet`)에서 재생 진행률을 계산할 때 0으로 나누는 문제가 생기지 않도록 방어 코드를 한 줄 추가했어요.
- Playwright(자동화 브라우저)로 실제로 버튼 클릭 → 지도 드래그 → 카테고리 선택 → 입력 → 등록 → 메인 화면에 새 핀이 뜨는 것까지 전체 흐름을 확인했어요. 콘솔 에러 없음.

**바뀐 파일들**
- `components/otr/OtrHeader.tsx` — "+ 장소 추가" 버튼(링크) 추가
- `app/add-place/page.tsx` (신규) — 장소 추가 화면 전체
- `components/otr/AddPlaceMap.tsx` (신규) — 화면 중앙에 핀이 고정된 지도 컴포넌트
- `components/otr/CategoryDial.tsx` (신규) — 가로 스크롤 스냅 방식의 카테고리 다이얼
- `data/otr/spots.ts` — 새 스팟을 추가하는 `addSpot` 함수 추가
- `components/otr/SpotSheet.tsx` — 오디오 길이가 0일 때 재생바 계산이 깨지지 않도록 방어 코드 추가

**기억해둘 점 (초보자 팁)**
- 이 시점에는 아직 서버(백엔드)나 데이터베이스가 없어서, 새로 등록한 장소는 브라우저를 새로고침하면 사라졌어요. (바로 위 항목에서 localStorage로 저장하도록 고쳤어요.)
- "다이얼"은 회전하는 기계식 다이얼이 아니라, **가로로 스크롤하다가 가운데 칸에 딱 맞춰 멈추는(스냅되는) UI**로 구현했어요. 모바일에서 흔히 쓰는 방식이에요.

### 2026-08-18 — README.md를 실습용 템플릿 문구에서 실제 OTR 서비스 소개로 교체

**무엇을 했나요?**
- GitHub 저장소에 있던 `README.md`가 아직 "CAMPUS AX-TON 실습용 스타터 템플릿" 그대로였어요.
  이걸 지금 만들고 있는 OTR 서비스 소개(뭘 하는 앱인지, 주요 기능, 기술 스택, 로컬 실행법, 폴더 구조)로 새로 썼어요.
- 실제 지도 화면 스크린샷을 `docs/screenshot.png`로 추가해서 README에 넣었어요.

**바뀐 파일들**
- `README.md` — 전체 재작성
- `docs/screenshot.png` (신규) — README에 들어가는 스크린샷

### 2026-08-18 — 가짜 지도를 진짜 카카오맵으로 교체, 내 위치 표시 기능 추가 ✅ (배포 사이트에서 동작 확인 완료)

**무엇을 했나요? (요약)**
- 화면 속 SVG로 대충 그려진 "가짜 지도"를 실제 **카카오맵(Kakao Maps)** 으로 교체했어요.
- 브라우저 위치 권한을 요청해서, 허용하면 **내 현재 위치가 파란 점**으로 지도 위에 표시돼요.
  지도 오른쪽 아래엔 "내 위치로 이동" 버튼도 추가했어요.
- 스팟(가게) 데이터의 위치를 화면 속 퍼센트 좌표 대신 실제 위도/경도(`lat, lng`)로 바꿨어요.
  단, 정확한 실제 주소는 아직 없어서 **대구 약전골목 근처에 임시로 배치**해둔 상태예요.
- 배포 사이트(`https://otr-lion.vercel.app`)에서 지도가 안 보이던 문제도 같이 해결했어요. 원인은 두 가지:
  1. 카카오 개발자 콘솔에서 이 앱의 **"카카오맵" 제품**이 꺼져 있었음 → 켜서 해결
  2. 카카오 키는 로컬(`.env.local`)에만 있고 **Vercel 배포 사이트엔 전달이 안 됐음** → Vercel 프로젝트 환경변수에 키를 추가하고 재배포해서 해결
- 자동화된 브라우저 점검(Playwright)과 실제 화면 확인으로, 배포 사이트에서 지도·핀·내 위치까지 전부 정상 동작하는 걸 확인했어요.

**바뀐 파일들**
- `components/otr/LocalMap.tsx` — 지도 컴포넌트를 카카오맵 기반으로 새로 작성
- `data/otr/spots.ts` — 각 스팟의 좌표를 `x, y`(퍼센트) → `lat, lng`(실제 위/경도)로 변경
- `app/page.tsx` — 상단 안내 문구를 "영남대 인근" → "약전골목 인근"으로 수정
- `.env.local` (신규) — 카카오맵 키를 넣는 로컬 환경변수 파일
- `types/kakao-maps.d.ts` (신규) — 카카오맵 SDK를 TypeScript에서 인식하게 해주는 타입 파일
- Vercel 프로젝트 환경변수에 `NEXT_PUBLIC_KAKAO_MAP_KEY` 추가 (코드 파일은 아니지만 배포에 꼭 필요한 설정)

**기억해둘 점 (초보자 팁)**
- 로컬(`localhost:3000`)과 배포 사이트(Vercel)는 **서로 다른 환경**이에요. `.env.local`은 내 컴퓨터에서만 쓰이고,
  배포 사이트에 반영하려면 Vercel 프로젝트 설정(또는 `vercel env add`)에 **따로** 등록해야 해요.
- 환경변수를 새로 추가/수정했으면 **반드시 재배포**해야 사이트에 반영돼요.

**주의할 점 / 아직 임시인 부분**
- 스팟 좌표는 실제 가게 위치가 아니라 **약전골목 근처에 대충 흩뿌려둔 임시 좌표**예요. 나중에 실제 위치로 교체 필요.
- 위치 권한을 사용자가 거부하면 "위치 권한을 허용하면 내 위치가 표시돼요" 안내 문구만 뜨고, 앱이 멈추거나 에러가 나진 않아요.
