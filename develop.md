# OTR 개발 일지

이 문서는 OTR 프로젝트를 만들면서 "무엇을, 왜, 어떻게" 바꿨는지 기록하는 개발 일지예요.
개발을 처음 배우는 사람도 따라올 수 있도록 최대한 쉬운 말로 씁니다.

- 최신 변경사항이 **맨 위**에 오도록 계속 추가할게요.
- 코드를 직접 안 열어봐도 이 파일만 읽으면 "지금 앱이 어떤 상태인지" 알 수 있는 걸 목표로 해요.

---

## 이 프로젝트는 뭔가요?

**OTR (Off The Record)** 은 동네 골목이나 오래된 가게에 숨은 이야기를 오디오로 들려주는
하이퍼로컬(아주 좁은 지역 기반) 오디오 가이드 앱이에요.

- 프레임워크: **Next.js** (리액트 기반 웹 프레임워크)
- 스타일: **Tailwind CSS**
- 언어: **TypeScript**

## 폴더 구조 (대략)

```
app/                Next.js 페이지들 (화면 단위)
  page.tsx          메인 홈 화면 (지도 + 카테고리 필터 + 코스 목록)
  recycle/          다른 페이지
components/otr/     화면을 구성하는 부품(컴포넌트)들
data/otr/           스팟(가게), 코스 같은 데이터
```

## 지금 실행해보는 법

```bash
npm run dev
```

실행 후 브라우저에서 `http://localhost:3000` 접속

## 앞으로 할 일 (TODO)

- [x] 카카오 개발자 사이트에서 JavaScript 키 발급받아 등록하기
- [x] 카카오맵 개발자 콘솔에 `http://localhost:3000` / 실제 배포 도메인(`https://otr-lion.vercel.app`) 등록하기
- [ ] 스팟(가게) 좌표를 임시값 대신 실제 정확한 주소/좌표로 교체하기

---

## 변경 기록

### 2026-08-18 — 가짜 지도를 진짜 카카오맵으로 교체, 내 위치 표시 기능 추가 ✅ (배포 사이트에서 동작 확인 완료)

**무엇을 했나요? (요약)**
- 화면 속 SVG로 대충 그려진 "가짜 지도"를 실제 **카카오맵(Kakao Maps)** 으로 교체했어요.
- 브라우저 위치 권한을 요청해서, 허용하면 **내 현재 위치가 파란 점**으로 지도 위에 표시돼요.
  지도 오른쪽 아래엔 "내 위치로 이동" 버튼도 추가했어요.
- 스팟(가게) 데이터의 위치를 화면 속 퍼센트 좌표 대신 실제 위도/경도(`lat, lng`)로 바꿨어요.
  단, 정확한 실제 주소는 아직 없어서 **대구 약전골목 근처에 임시로 배치**해둔 상태예요.
- 배포 사이트(`https://otr-lion.vercel.app`)에서 지도가 안 보이던 문제도 같이 해결했어요. 원인은 두 가지:
  1. 카카오 개발자 콘솔에서 이 앱의 **"카카오맵" 제품**이 꺼져 있었음 → 켜서 해결
  2. 카카오 키는 로컬(`.env.local`)에만 있고 **Vercel 배포 사이트엔 전달이 안 됐음** → Vercel 프로젝트 환경변수에 키를 추가하고 재배포해서 해결
- 자동화된 브라우저 점검(Playwright)과 실제 화면 확인으로, 배포 사이트에서 지도·핀·내 위치까지 전부 정상 동작하는 걸 확인했어요.

**바뀐 파일들**
- `components/otr/LocalMap.tsx` — 지도 컴포넌트를 카카오맵 기반으로 새로 작성
- `data/otr/spots.ts` — 각 스팟의 좌표를 `x, y`(퍼센트) → `lat, lng`(실제 위/경도)로 변경
- `app/page.tsx` — 상단 안내 문구를 "영남대 인근" → "약전골목 인근"으로 수정
- `.env.local` (신규) — 카카오맵 키를 넣는 로컬 환경변수 파일
- `types/kakao-maps.d.ts` (신규) — 카카오맵 SDK를 TypeScript에서 인식하게 해주는 타입 파일
- Vercel 프로젝트 환경변수에 `NEXT_PUBLIC_KAKAO_MAP_KEY` 추가 (코드 파일은 아니지만 배포에 꼭 필요한 설정)

**기억해둘 점 (초보자 팁)**
- 로컬(`localhost:3000`)과 배포 사이트(Vercel)는 **서로 다른 환경**이에요. `.env.local`은 내 컴퓨터에서만 쓰이고,
  배포 사이트에 반영하려면 Vercel 프로젝트 설정(또는 `vercel env add`)에 **따로** 등록해야 해요.
- 환경변수를 새로 추가/수정했으면 **반드시 재배포**해야 사이트에 반영돼요.

**주의할 점 / 아직 임시인 부분**
- 스팟 좌표는 실제 가게 위치가 아니라 **약전골목 근처에 대충 흩뿌려둔 임시 좌표**예요. 나중에 실제 위치로 교체 필요.
- 위치 권한을 사용자가 거부하면 "위치 권한을 허용하면 내 위치가 표시돼요" 안내 문구만 뜨고, 앱이 멈추거나 에러가 나진 않아요.
